# Data

Store local datasets under `raw/`, `interim/`, and `processed/`. Their contents
are ignored by Git; only the placeholder files are tracked.

Every project should document where its data came from, its license, and how to
download or regenerate it. Do not place sensitive or personal data here.
