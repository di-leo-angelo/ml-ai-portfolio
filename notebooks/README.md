# Notebooks

This directory is for cross-project exploration and demonstrations. Keep
project-specific notebooks inside the relevant `projects/<name>/` directory.

Before committing a notebook:

- Restart the kernel and run all cells in order.
- Remove credentials, private data, and machine-specific paths.
- Make the objective and conclusions clear in Markdown cells.
- Move reusable logic into `src/ml_ai_portfolio/`.
