"""Leakage-safe preprocessing and model pipeline definitions.

TODO:
    - Select numeric and categorical columns explicitly.
    - Build separate preprocessing pipelines for each type.
    - Combine them with ColumnTransformer.
    - Place preprocessing and the estimator in one sklearn Pipeline.
    - Define a dummy baseline and a small set of candidate classifiers.
"""
